# 验收记录：标签页与 RBAC 分配

## 范围

- 顶部标签页使用 SortableJS 拖拽排序。
- 仪表盘标签固定在最左侧，不参与拖拽。
- 标签顺序和激活状态刷新后持久化。
- 角色菜单与权限分配支持半选状态。
- 标签关闭、路由和缓存行为回归检查。

## 环境

- 项目：`web-admin`
- 日期：2026-05-07
- 验证时分支：`main`

## 自动化验证

- 后端：在 `backend/` 下运行 `python -m pytest tests -v`
  - 结果：`24 passed`
- 前端：在 `web-admin/` 下运行 `npm --prefix frontend run build`
  - 结果：通过，仅保留既有 chunk-size 警告。

## 人工检查

- 拖拽标签后顺序立即更新。
- 拖拽过程中的视觉反馈正常。
- 当前激活标签保持不变，页面状态不丢失。
- 仪表盘标签始终固定在最左侧。
- 关闭当前、关闭其他、关闭全部行为正确。
- 标签过多时可以横向滚动，并仍可排序。
- 刷新后恢复标签集合、顺序和激活标签。
- 菜单与权限分配支持半选状态和联动选择。
- 保存后显示成功提示，刷新列表并正确回显。

## 结论

2026-05-07，标签页持久化/拖拽和角色菜单-权限分配流程验收通过。
