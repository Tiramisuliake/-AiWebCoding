# 验收记录（2026-05-07）

## 本次验收范围
- 顶部标签页拖拽排序（SortableJS）
- 首页标签固定最左（`dashboard` 不参与拖拽）
- 标签顺序/激活状态持久化与刷新恢复
- 角色“分配菜单及权限”半选/联动交互
- 关闭标签与路由/缓存逻辑非回归

## 验收环境
- 仓库：`web-admin`
- 时间：2026-05-07（Asia/Hong_Kong）
- 分支：`main`

## 自动化验证结果
- 后端测试：`python -m pytest tests -v`（在 `backend/` 目录执行）  
  - 结果：`24 passed`
- 前端构建：`npm --prefix frontend run build`  
  - 结果：构建通过（仅保留既有 chunk size 警告，无功能阻断）

## 人工回归检查结果
- [x] 拖拽标签后顺序立即更新
- [x] 拖拽过程中视觉反馈正常（拖拽态/占位态）
- [x] 当前激活标签状态保持不变，页面内容不丢失
- [x] 首页标签始终固定在最左，不可拖离首位
- [x] 标签关闭（关闭当前/其他/全部）行为正常
- [x] 标签超长可横向滚动，拖拽跨可视区域仍可排序
- [x] 刷新后恢复标签集合、顺序、激活标签
- [x] 菜单及权限分配支持半选状态与联动勾选
- [x] 保存后提示成功并刷新列表，回显正常

## 结论
- 本轮“拖拽 + 半选 + 关闭 + 刷新恢复”验收通过，可继续在当前结构上迭代后续需求。

